<?xml version="1.0" encoding="UTF-8"?>
<project name="org.eclipse.pde.build" default="main" basedir="..">

	<property name="bootclasspath" value=""/>

	<target name="main">
		<condition property="fullBuild">
			<equals arg1="${buildType}" arg2="full"/>
		</condition>
		<antcall target="build"/>
	</target>

	<target name="build" if="fullBuild">
		<antcall target="clean"/>
		<antcall target="lib/pdebuild-ant.jar"/>
		<antcall target="refresh"/>
	</target>

	<target name="init" depends="properties">
		<property name="plugin" value="org.eclipse.pde.build"/>
		<property name="temp.folder" value="${basedir}/temp.folder"/>
		<property name="plugin.destination" value="${basedir}"/>
		<property name="build.result.folder" value="${basedir}"/>
	</target>

	<target name="properties" if="eclipse.running">
		<property name="build.compiler" value="org.eclipse.jdt.core.JDTCompilerAdapter"/>
	</target>

	<target name="lib/pdebuild-ant.jar" depends="init" unless="lib/pdebuild-ant.jar">
		<property name="destdir" value="${temp.folder}/lib/pdebuild-ant.jar.bin"/>
		<delete dir="${temp.folder}/lib/pdebuild-ant.jar.bin"/>
		<mkdir dir="${temp.folder}/lib/pdebuild-ant.jar.bin"/>
		<!-- compile the source code -->
		<javac destdir="${temp.folder}/lib/pdebuild-ant.jar.bin" failonerror="false" verbose="false" debug="on" includeAntRuntime="no" bootclasspath="${bootclasspath}" classpath="${basedir}/../org.eclipse.core.runtime/runtime.jar;${basedir}/../org.apache.ant/ant.jar;${basedir}/../org.eclipse.update.core/bin;${basedir}/../org.eclipse.jdt.core/jdtcore.jar;${basedir}/../org.eclipse.ant.core/antsupport.jar;${basedir}/../org.eclipse.update.core/updatecore.jar;${basedir}/../org.eclipse.jdt.core/bin;${basedir}/bin;${basedir}/../org.eclipse.core.boot/boot.jar;${basedir}/../org.eclipse.ant.core/bin;${basedir}/../org.eclipse.core.runtime/bin;${basedir}/../org.eclipse.core.boot/bin">
			<src path="src_ant/"/>
		</javac>
		<!-- copy necessary resources -->
		<copy todir="${temp.folder}/lib/pdebuild-ant.jar.bin">
			<fileset dir="src_ant/" excludes="**/*.java"/>
		</copy>
		<mkdir dir="${build.result.folder}/lib"/>
		<jar jarfile="${build.result.folder}/lib/pdebuild-ant.jar" basedir="${temp.folder}/lib/pdebuild-ant.jar.bin"/>
		<delete dir="${temp.folder}"/>
	</target>

	<target name="clean" depends="init">
		<delete file="${build.result.folder}/lib/pdebuild-ant.jar"/>
		<delete dir="${temp.folder}"/>
	</target>

	<target name="refresh" depends="init" if="eclipse.running">
		<eclipse.refreshLocal resource="${plugin}" depth="infinite"/>
	</target>

</project>